# Models

The adapter layer is intentionally model-agnostic. CJKE, Yamine-style multilingual models, and OpenUTAU multilingual conventions can be implemented behind adapters. The actual model's `phonemes`, `languages`, language IDs, dictionaries, and configuration are authoritative.
