# Outputs

Expected artifacts include `train.log`, `metrics.jsonl`, `training_state.json`, `events.jsonl`, and dry-run reports. Large/generated artifacts should be synchronized to persistent storage rather than committed to Git.
